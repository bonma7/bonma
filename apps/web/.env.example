# Node Environment
NODE_ENV=development

# API URL Configuration
# This is used only during Render deployment to generate the runtime config.json
# For local development, edit apps/web/public/config.json directly
# API_URL=https://your-api-url.onrender.com
